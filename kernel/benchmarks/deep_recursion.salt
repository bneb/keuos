
fn recursive_depth(n: i32) -> i32 {
    if n <= 0 {
        return 0;
    }
    return 1 + recursive_depth(n - 1);
}

fn main_salt() -> i32 {
    // Stress test: 1000-deep recursion to exercise stack depth.
    return recursive_depth(1000); 
}
