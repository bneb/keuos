package kernel.core.timing

// Timing Utilities
// Relies on assembly stub in boot.S (renamed to avoid salt-front intrinsic lowering)

extern fn get_cycles_asm() -> u64;

pub fn get_cycles() -> u64 {
    return get_cycles_asm();
}
