package kernel.core.io

// Low-level Port I/O stubs.
// Linked to assembly implementations in boot.S or io_stubs.S

pub extern fn outb(port: u16, val: u8);
pub extern fn inb(port: u16) -> u8;
pub extern fn outw(port: u16, val: u16);
